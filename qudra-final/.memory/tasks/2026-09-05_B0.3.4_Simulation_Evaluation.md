# Task: B0.3.4 & B0.3.5 — SimulationPage & EvaluationPage + SplashScreen Reload Update

- **التاريخ**: 2026-09-05
- **الحالة**: `DONE_UNVERIFIED` (بانتظار المعاينة البصرية من المستخدم)
- **الملفات المنشأة والمعدلة**:
  - `src/components/layout/SplashScreen.tsx` (تحديث سلوك الـ Reload)
  - `src/pages/simulation/SimulationPage.tsx` (جديد)
  - `src/pages/simulation/SimulationPage.module.css` (جديد)
  - `src/pages/evaluation/EvaluationPage.tsx` (جديد)
  - `src/pages/evaluation/EvaluationPage.module.css` (جديد)
  - `src/contexts/ProblemContext.tsx` (إضافة simulationAnswers)
  - `src/app/router/routes.tsx` (تسجيل مساري /simulation و /evaluation)
  - `DECISIONS.md` (إضافة القرارين 007 و 008)
  - `PROJECT_MAP.md` (توثيق المكونين الجديدين)
  - `TASKS.md` (تحديث حالة B0.3.4 و B0.3.5 إلى DONE_UNVERIFIED)
  - `STATUS.md` (تحديث ملخص المرحلة والإنجاز)
  - `CHANGELOG.md` (إضافة سجل التغييرات)
  - `TEST_REPORT.md` (سجل الفحص الشامل كأدمن بالأدلة)

---

## 1. Goal (الهدف)
1. تعديل `SplashScreen` لتظهر عند كل Reload للمتصفح دون إلغاء منطق `sessionStorage` (معلق وموثق في `DECISIONS.md`).
2. بناء شاشة المحاكاة `SimulationPage` (`/simulation`) مطابقة حرفياً لـ `prototype.html`: `simIntro`، المهام الثلاث، المؤقت 872 ثانية (14:32) بنظام pause/resume عبر `visibilitychange`، عداد الكلمات ومؤشرات `sig3`، وشاشة `grading` الانتقالية بمراحلها الأربع.
3. بناء شاشة التقييم `EvaluationPage` (`/evaluation`) مطابقة حرفياً: دونت SVG بنصف قطر 86، محيط 540.4، عداد 85، المعايير الأربعة وقاعدة البرتقالي للأقل من 80، نقاط القوة والضعف، تفصيل الإجابات الثلاث الحقيقية، ومعالجة زر النهاية لمنع Dead UI (القرار 008).

## 2. Changes Made (التغييرات المنفذة)
1. **SplashScreen**: تعليق فحص وكتابة `sessionStorage` مؤقتاً لأغراض بيئة التطوير في B0 مع الاحتفاظ به.
2. **SimulationPage**:
   - `simIntro`: الخطوة 3 من 6، الترويسة، المعايير الأربعة، صندوق النصيحة، وزر البداية.
   - `simRun`: شريط التقدم، مؤقت 14:32 (872s) بنمط `useRef` + `useEffect` للتنظيف النظيف، وفئة `.low` عند الوصول لدقيقة أو أقل، وإيقاف/استئناف عند `visibilitychange`.
   - المهام الثلاث الأصلية: Product Thinking, UI/UX, Backend مع الأسئلة والملاحظات التوجيهية.
   - مؤشرات `sig3`: طول كافٍ (`words >= 30`)، فيها تبرير Regex، وفيها تحديد Regex.
   - شاشة `grading`: المراحل الأربع (260ms / 640ms / 1020ms / 1400ms) ثم الانتقال المباشر لـ `/evaluation`.
3. **EvaluationPage**:
   - الخطوة 4 من 6، الترويسة والوصف.
   - دونت SVG مع أنيميشن الإزاحة وتصاعد العداد لـ 85 خلال 900ms.
   - المعايير الأربعة مع فئة `.low` وشريط التقدم البرتقالي لـ «بنية الحل» (75%).
   - نقاط القوة الثلاث ونقطتا الضعف.
   - تفصيل الإجابات الثلاث مع إجابات المرشح الحقيقية من `simulationAnswers`.
   - زر «حوّل النتيجة إلى قدرات مثبتة» مع تنبيه حالة B0.3.6 لمنع Dead UI (القرار 008).
4. **Context & Router**:
   - إضافة `simulationAnswers` في `ProblemContext.tsx`.
   - تسجيل مساري `/simulation` و `/evaluation` في `routes.tsx`.

## 3. Verification via Real Chrome CDP (الأدلة ونتائج الفحص الفعلي)
1. **SplashScreen Reloads**: ظهور الشاشة في 3 عمليات Reload متتالية مع فحص DOM لكل منها (`exists: true, hasHalo: true, hasLock: true`).
2. **عد المؤقت الحقيقي**: تسجيل القيمة الابتدائية `14:32` ثم بعد 2.5s وصلت لـ `14:29`.
3. **فحص pause/resume**: توقف المؤقت تماماً عند `document.hidden = true` عند `14:29` لمدة ثانيتين، واستئناف العد عند العودة وصولاً لـ `14:27`.
4. **فحص كتابة الإجابات الحقيقية**: إدخال نصوص واقعية في المهام الثلاث، وتنشيط مؤشرات `why` و `spec` بنجاح.
5. **شاشة Grading**: تفعيل المودال والانتقال التلقائي بعد 1820ms إلى `/evaluation`.
6. **فحص EvaluationPage**:
   - Donut وعداد 85 معايير كاملة.
   - «بنية الحل» تحمل فئة `.low` وشريط التقدم باللون البرتقالي (75%).
   - تفصيل الإجابات الثلاث يعرض نفس النصوص الحقيقية التي كتبها المستخدم.
7. **منع Dead Link**: النقر على زر التحويل لم يغير المسار وأظهر تنبيه: `ℹ️سِجلّ القدرات (Skill DNA) — قريباً في المرحلة التالية (B0.3.6)`.
8. **فحص سلوك انتهاء الوقت**: تم اختبار انتهاء الوقت التلقائي (3 ثوانٍ) وإثبات تعطيل حقل الكتابة، إظهار التنبيه، حفظ الإجابة الحالية تلقائياً، والتشغيل التلقائي للتقييم، ثم استعادة المؤقت الأصلي (872s).
9. **Console**: 0 أخطاء (Console نظيفة تماماً).
10. **بناء المشروع**: نجاح `npm run build` في 1.69 ثانية دون أي أخطاء أو تحذيرات.

## 4. Next Steps (الخطوة التالية)
- الانتقال إلى المهمة B0.3.6 (`SkillDNAPage` - سِجلّ القدرات وبصمة المهارات التفاعلية `/skill-dna`).
