# Task: B0.3.2 & B0.3.3 — Splash Screen, ProblemInputPage & CapabilitiesPage

- **التاريخ**: 2026-09-05
- **الحالة**: `DONE_UNVERIFIED` (بانتظار المعاينة البصرية من المستخدم)
- **الملفات المنشأة والمعدلة**:
  - `src/components/layout/SplashScreen.tsx`
  - `src/components/layout/SplashScreen.module.css`
  - `src/pages/problem/ProblemInputPage.tsx`
  - `src/pages/problem/ProblemInputPage.module.css`
  - `src/pages/capabilities/CapabilitiesPage.tsx`
  - `src/pages/capabilities/CapabilitiesPage.module.css`
  - `src/contexts/ProblemContext.tsx`
  - `src/app/providers/AppProviders.tsx`
  - `src/app/router/routes.tsx`
  - `src/app/App.tsx`

---

## 1. Goal (الهدف)
حل المشكلتين الحرجتين المكتشفتين من قبل المستخدم:
1. ظهور شاشة الإقلاع الافتتاحية (Splash Screen) بهالتها وشعارها وتوقيتها الحرفي (2.75s).
2. جعل زر «حلّل المشكلة» تفاعلياً حقيقياً يطلق شاشة التحليل المرحلية الست وينقل لصفحة القدرات `/capabilities`.

## 2. Changes Made (التغييرات المنفذة)
1. بناء `SplashScreen` مطابقاً للنموذج الأصلي في `prototype.html` مع مراعاة `sessionStorage` وتفضيل `prefers-reduced-motion`.
2. إعادة بناء `ProblemInputPage.tsx` بـ State حي لـ Textarea، وأزرار الأمثلة الثلاثة الجاهزة، والتحقق الأدنى من الطول (15 حرفاً)، وشاشة التحليل المرحلية الست (`Analyzing Overlay`).
3. بناء `CapabilitiesPage.tsx` بالبطاقات الخمس والملخص والأولويات والاستثناءات.
4. إنشاء `ProblemContext.tsx` لمشاركة حالة نص المشكلة والقدرات المستخرجة.
5. تسجيل المسار `/capabilities` في `routes.tsx`.

## 3. Verification via Real Chrome CDP (نتائج الفحص الآلي بالمتصفح الحقيقي)
1. ظهور شاشة Splash فوراً عند فتح جلسة جديدة واختفاؤها بعد 2.75s + 0.55s.
2. فحص التحقق من النص القصير: ظهور رسالة الخطأ وتحول الإطار للأحمر.
3. فحص أزرار الأمثلة: تعبئة النص وإزالة الخطأ.
4. فحص شاشة التحليل: ظهورها بالمراحل الست وتقدم شريط التعبئة والانتقال لـ `/capabilities`.
5. فحص محتوى `/capabilities`: العنوان والبطاقات الخمس وزر المحاكاة.
6. سجل الـ Console: صفر أخطاء وصفر استثناءات.
7. بناء المشروع `npm run build`: نجح في 1.75 ثانية دون أي خطأ.

## 4. Next Steps (الخطوة التالية)
- معاينة المستخدم البصرية المباشرة على `http://localhost:5173/`.
- الانتقال إلى B0.3.4 (شاشة المحاكاة SimulationPage مع مؤقت الـ 15 دقيقة).
