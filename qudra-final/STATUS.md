# STATUS.md — ملخص الحالة الحالية

> **آخر تحديث**: 2026-09-07
> هذا الملف يُحدَّث بعد كل مهمة منجزة (راجع `قواعد.md` القسم 43 و46). يجب أن يعكس دائماً حقيقة آخر نقطة توقف فعلية، وليس النية أو الخطة.

---

## المرحلة الحالية

**B0 مكتملة ومدققة وموحّدة الهوية بنسبة 100% (B0 Fully Refined & Design-System Aligned)** — اكتمال تدقيق كامل شاشات المنصة والتحقق البصري والمعماري:
1. توحيد مصدر شعار قُدرة عبر مكوّن `QudraLogo.tsx` المستند حصرياً للأصول الأصلية (`qudra-icon.webp`, `qudra-wordmark.webp`, `qudra-wordmark-dark.webp`) وحذف ملفات SVG الزائفة.
2. تصنيف مكوّنات النظام وفق المسارين الصارمين (المسار أ منسوخ حرفياً / المسار ب مبني على التوكنز).
3. تصحيح أزرار `ProfilePage` الثلاثة وبطاقات المصادر وتحديث Design Tokens في `tokens.css`.
4. اجتياز بوابات الفحص الصارمة: `typecheck: 0`, `lint: 0`, `build: 0` في 1.76 ثانية.

---

## آخر إنجاز فعلي (2026-09-07 — توحيد الشعار وتصحيح الأزرار والمكوّنات)

1. **توحيد مصدر الشعار وحذف الملفات المشبوهة (`QudraLogo.tsx`)**:
   - إثبات عدم صحة ملفي `qudra-mark.svg` و `qudra-mark-dark.svg` وحذفهما نهائياً.
   - بناء مكوّن `QudraLogo.tsx` وتعميمه عبر 6 شاشات رئيسية بحجم واستدعاء منضبط.
2. **تصحيح نظام الأزرار وفق `design-system.html`**:
   - بناء فئة `.linkbtn` بحواف كاملة (`border-radius: 999px`) وانتقال سلس وألوان هوية النظام (`var(--proof-text)`, `var(--gap-text)`).
   - تصحيح أزرار "عرض مسار التطور ←"، "سد الفجوة"، "افتح تفاصيل الدليل ←"، وأزرار الإدارة والإضافة `.act`.
3. **تحديث Design Tokens في `tokens.css`**:
   - إضافة الأسماء الحرفية الرسمية (`--f-display`, `--f-body`, `--f-mono`, `--ground`, `--surface`, `--shadow`).
4. **بناء مكوّن BottomNav للهواتف الذكية وحل التمرير الأفقي في TopBar**:
   - حل الخلل الحرج بغياب التنقل السفلي لمستخدمي الهواتف عند مقاسات `≤56rem` (896px).
   - توفير مساحات لمس مطابقة للمعايير العالمية (`≥44px`)، وتأثير زجاجي ضبابي (`backdrop-filter: blur(16px)`).
   - دعم التبديل التلقائي بين مسار صاحب المشكلة ومسار صاحب القدرة.
   - إضافة هامش حماية سفلي في `MainLayout.module.css` لمنع حجب الأزرار الرئيسية.
2. **إصلاح استجابة الشريط العلوي (`TopBar.module.css`)**:
   - القضاء التام على شريط التمرير الأفقي غير المقصود (Horizontal Overflow) على شاشات 375px و414px.
   - إخفاء اسم المستخدم على الموبايل لتوفير المساحة، وإحكام أزرار تبديل الدور.
3. **تصحيحات المحاذاة والطباعة العربية (RTL & Typography)**:
   - تصحيح خاصية `border-left` إلى `border-inline-start` في `SkillDnaPage.module.css` لضمان اتساق RTL.
   - تطبيق كلاس `num` على أرقام النسب والدرجات وكلاس `mono` على ترقيم المراحل 01–04 في `ResultPage` و`SkillDnaPage` و`CandidatesPage` و`CandidateDetailPage` و`ReRankingPage`.
4. **تثبيت وتفعيل المهارات (Agent Skills)**:
   - تثبيت حزمة `motion` لدعم الحركات الدقيقة.
   - فحص استهلاك التوكنز عبر `codeburn status` وتوثيق الأرقام بدقة (`Today $1.97 245 calls`).
   - تنفيذ تدقيق تقليل التعقيد عبر `ponytail-audit` وإزالة الملف المكرر غير المرغوب فيه `.md`.
5. **اجتياز بوابات الجودة (Quality Gates)**:
   - `npm run typecheck`: صفر أخطاء (TypeScript strict).
   - `npm run lint`: صفر أخطاء (ESLint v9 flat config).
   - `npm run build`: نجاح كامل في 1.95 ثانية.
6. **الفحص الآلي والاختبار الشامل عبر Chrome CDP**:
   - فحص جميع الشاشات والمسارات الـ 23 واجتيازها بنجاح تام (`true`).
   - اختبار التفاعل الحي: تبديل الأدوار، الوضع الداكن، قفزة تحدي المرشح، والكونسول نظيف بنسبة 100% (0 أخطاء).
   - اجتياز فحص التايب سكربت `npx tsc --noEmit` بصفر أخطاء.

---

## الخطوة التالية المتوقعة

- تقديم العمل للمستخدم واستعراض المنصة أو الانتقال لتحسينات إضافية حسب الرغبة.

---

## عوائق/نقاط معلّقة

- تحقق من وجود ملف `qudra-figma-tokens.json` فعلياً قبل الاعتماد عليه (راجع `.memory/notes.md`).
- لم يُختبر بعد فعلياً نجاح تحميل جميع المهارات المذكورة في `SKILLS.md` عبر أوامر `npx skills` داخل بيئة Bolt الفعلية عند بدء التنفيذ.
- الملفات القديمة المكررة (نسختا `AGENTS.md`/`CLAUDE.md` المتعارضتين سابقاً، نسخ `QUDRA_FINAL_BOLT_PROMPT` المتعددة) أُرشفت في `docs/archive/` — لا تُقرأ من الوكيل.
